# Robolectric API level for the local unit tests.
#
# Running against API 36+ requires Java 21 (Robolectric reports "Android SDK 36 requires
# Java 21" on older JDKs), so the build and CI use JDK 21.
sdk=36
