# Copy to keystore.properties (gitignored) and fill in to sign release builds locally.
# The GitHub Actions release workflow writes this file from repository secrets instead.
#
# storeFile may be absolute or relative to the repository root.
storeFile=release.jks
storePassword=changeit
keyAlias=release
keyPassword=changeit
